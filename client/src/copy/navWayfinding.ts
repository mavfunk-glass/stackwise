/**
 * Shared navigation UI: monochrome glyphs (see NavIcon) for a uniform, on-brand look.
 */
export type { NavIconKind } from '../components/NavIcon';
export { default as NavIcon } from '../components/NavIcon';
