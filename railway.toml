[build]
builder = "RAILPACK"
buildCommand = "npm ci --prefix client && npm ci --prefix server && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/health"
